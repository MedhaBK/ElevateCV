You are an expert technical recruiter and career coach.

A candidate has submitted their resume for a job. Below are both texts.
Analyze the match and return a JSON object with exactly this structure:

{
  "matched_skills": ["<skill present in both resume and JD>"],
  "missing_skills": ["<skill required in JD but absent in resume>"],
  "extra_skills": ["<valuable skill in resume not asked for in JD>"],
  "experience_gap": "<1-2 sentences on experience level mismatch if any>",
  "recommendations": ["<actionable tip 1>", "<actionable tip 2>", "<actionable tip 3>"]
}

Rules:
- Return ONLY the JSON object. No explanation, no markdown, no code fences.
- Be specific — name actual technologies, tools, and skills.
- Every array must have at least 2 items.
- Keep recommendations actionable and specific to this JD.

RESUME:
{resume_text}

JOB DESCRIPTION:
{jd_text}